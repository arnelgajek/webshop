import React from 'react';

class EmailAddress extends React.Component {
    render() {
        return (
            <a href="/" className="" style={{color:'black'}}><i className="ui envelope icon"></i>info@webshop.se</a>
        );
    }
}

export default EmailAddress;